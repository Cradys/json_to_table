# Json to table view

Convert json file into tables with data models, saves refs betweeen models, merge objects in array


## Installation

#### Prerequisites

Need to install uv package:
`pip install uv`

#### How to start
```
pip install uv
uv sync
source .venv/bin/activate
```

Now you can start using `uv run main.py [file_name]` or `bash main.sh` (contains same comand)

## Usage
1. Place files .json in /jsons, 2 examples already ther
2. Run `uv run main.py [file_name]`
3. After processing will be created folder /converted_table, wich will contain converted json`s file in .md format

<img width="500" height="1000" alt="image" src="https://github.com/user-attachments/assets/974381e4-547a-42ab-a970-ed2944b54487" /><img width="500" height="1000" alt="image" src="https://github.com/user-attachments/assets/9ef3ca8b-53dd-49b5-8dd6-01fd3ae09a9e" />

